# Wisper Clone Releases

This repository publishes signed Windows update artifacts for Wisper Clone.
It intentionally contains no application source code.

`latest.json` is the public Tauri updater manifest. Each `releases/vX.Y.Z/`
directory is an immutable snapshot containing that version's manifest,
installers, and detached signatures.

The application source repository remains private.
